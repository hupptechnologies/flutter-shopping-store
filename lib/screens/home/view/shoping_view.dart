import 'package:flutter/material.dart';

class ShopingView extends StatelessWidget {
  const ShopingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Shoping view'),
    );
  }
}
